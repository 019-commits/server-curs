services:
  - type: web
    name: loyalty-swift-rates
    runtime: node
    plan: free

    buildCommand: npm install

    startCommand: npm start

    envVars:
      - key: NODE_ENV
        value: production
